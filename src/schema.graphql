type Query {
  users: [User]
}

type Mutation {
  createUser(input: UserInput): User
}

type User {
  id: String
  name: String!
  email: String!
  birthdate: String!
}

input UserInput {
  name: String!
  email: String!
  password: String!
  birthdate: String!
}
